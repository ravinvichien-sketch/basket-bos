-- ============================================================
-- Basket Bos — ไฟล์นี้ถูกยกเลิก (DEPRECATED — DO NOT RUN)
--
-- เป็นเวอร์ชันเก่าที่หลงเหลือจากการทำงานซ้ำ ใช้แนวทางคนละแบบ
-- (manager_profile_id / can_manage_game / hard delete) ซึ่งโค้ดแอป
-- ปัจจุบัน "ไม่ได้ใช้" แล้ว
--
-- เวอร์ชันที่ใช้จริงคือ:
--   017_delete_game_session.sql  — ลบนัดแบบ soft delete
--   018_group_admin_sessions.sql — สิทธิ์แอดมินก๊วนคุมนัด
--   019_acting_admin.sql         — มอบสิทธิ์คุมนัดชั่วคราว (acting admin)
--
-- อย่ารันไฟล์นี้ (เว้นว่างไว้โดยตั้งใจ)
-- ============================================================
